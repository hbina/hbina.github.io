{
  "scripts": {
    "serve": "resume-cli serve --resume resume.json --theme jsonresume-theme-stackoverflow"
  },
  "dependencies": {
    "jsonresume-theme-stackoverflow": "^2.0.0",
    "resume-cli": "^3.0.0",
    "resumed": "^3.0.0"
  }
}
